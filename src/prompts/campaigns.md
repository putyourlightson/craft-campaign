# Campaigns

Campaigns are custom elements that are provided by the “Campaign” plugin. Campaigns are represented by the `CampaignElement` class.

Each campaign belongs to exactly one “Campaign Type” as defined by the `campaignTypeId` attribute. Campaign types represented by the `CampaignTypeModel` class.

Campaign elements do NOT belong to sections.
