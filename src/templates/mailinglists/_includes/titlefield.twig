{% import '_includes/forms' as forms %}

{{ forms.textField({
    label: 'Title'|t('app')|e,
    id: 'title',
    name: 'title',
    value: mailingList.title,
    errors: mailingList.getErrors('title'),
    first: true,
    autofocus: true,
    required: true,
    pending: true,
    maxlength: 255
}) }}
