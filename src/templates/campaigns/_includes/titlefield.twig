{% import '_includes/forms' as forms %}

{{ forms.textField({
    label: 'Title'|t('app')|e,
    id: 'title',
    name: 'title',
    value: campaign.title,
    errors: campaign.getErrors('title'),
    first: true,
    autofocus: true,
    required: true,
    pending: true,
    maxlength: 255
}) }}
